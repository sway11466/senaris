# 上面＝割れた岩盤の段。1024px の原寸をそのまま切り抜く（面が大きいので切り出しは不要）。
powershell -File tools\gen_terrain_tile.ps1 bedrock assets\terrain-src\bedrock\bedrock_01_raw.png

# 側面＝層になった岩の断面。左右シームレスは全幅を保つ限り生きるので、行だけを切る。
# 高さ0.36の段差は盤で10px前後しかないので、帯を薄く切って層を大きく見せる（城壁 rampart_side と同じ理屈）。
# +884 は明度が画像全体の平均(62.20)に最も近い帯を14候補から選んだもの（差0.10）。
magick assets\terrain-src\bedrock\bedrock_side_01_raw.png -crop 1024x140+0+884 +repage -colors 64 -dither None assets\terrain\bedrock_side.png

# terrain_skin.csv: elevation 0.36（0.18刻み×2。台地0.18・城壁0.54の系列）/ orientable full。
# CSV を直したら JSON を焼き直す:
#   godot --headless --path . --script res://data/terrain/convert.gd

# 変種は1枚。明暗むら4.00＝大局の明暗が大きいが、山では斜面の起伏として働くので抑えない
# （平坦な地面の基準 1.8 は当てはめない）。広く敷いて繰り返しが気になったら mountain_2.png を足す。

# 没案: (1) 岩を1つずつ輪郭で囲んだ「転がった岩」＝砂利場に見えた。(2) 1ヘクスに山を1つ立てて描く
# 「昔のRPGの世界地図」方式＝1ヘクスが街区や家庭菜園を表す盤の縮尺と桁が合わない（山だけ1マスが数km）。
