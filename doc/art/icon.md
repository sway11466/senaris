# アプリアイコン

ウィンドウ・タスクバー・実行ファイルに出る絵の正本。ロゴの紋章から文字を落としたもので、モチーフ・配色・幾何は [logo.md](logo.md) が正本。作業ソースは `godot/assets/appicon-src/`。

---

## 寸法で2つを使い分ける

アイコンは 16px から 256px までの同じ絵を要求されるが、7枚のクラスタは小さい寸法でタイルの粒が潰れて塊になる。そこで小さいほうは中央ヘックス1枚に剣と杖を刺した版へ切り替える。

| 寸法 | 使う版 |
| --- | --- |
| 64px 以上 | 7枚のクラスタ（ロゴの紋章そのまま） |
| 48px 以下 | 中央ヘックス1枚＋剣と杖 |

1枚版もロゴの中央部分をそのまま切り出した形で、刺さり口の位置はロゴと同じ。中央ヘックスの幅はクラスタの中でも単独でも変わらないため、切り替えても絵の縮尺が跳ねない。

## 文字を落とす

タイルから文字と武器を抜いているマスクを、武器だけに絞って組み直す。文字のレイヤーを消すだけでは、タイルに文字型の切り欠きが残る。

## 正方形に収める

紋章は武器が上へ伸びるぶん縦長で、正方形ではない。7枚版は武器の長さを変えず、左右に余白を足して正方形にする。1枚版は武器を短くして枠に合わせる。

短くするときに効くのは傾きのほうで、長さではない。クロス点の高さは刺さり口の間隔と傾きだけで決まる（クロス点の高さ ＝ 刺さり口の高さ − 間隔 ÷ tan傾き）。武器を短くしても交点は動かず、交点から先だけが縮んで X が寸詰まりになる。1枚版の傾きをロゴ本体より深くしているのはこのため。

| 項目 | 値 |
| --- | --- |
| タイルの縮小率（隙間） | 0.90（ロゴの小サイズ版と同じ） |
| 7枚版の武器 | ロゴ本体と同じ長さ・傾き20度 |
| 1枚版の刺さり口 | 中央ヘックスの中心から ±30・下へ14（ロゴ本体と同じ） |
| 1枚版の武器 | ロゴ本体の 0.48 倍・傾き28度 |

## 色

暗背景版のパレット（[logo.md](logo.md)）をそのまま使う。7枚版はクラスタ全体を横切るグラデーション、1枚版はその1枚を左の鋼青から右の深紅へ横切らせる。剣と杖は無彩色のまま。

明るい地（エクスプローラや白いデスクトップ）では武器が地に沈む。武器を濃くすると今度は暗い地（タスクバー）で沈むので、両方の地で成立する武器の色は無い。タイルに色があるかぎり輪郭は残るため、暗い地を主に取って武器は明るいままにする。

## 保管と書き出し

| 段階 | 置き場 | 例 |
| --- | --- | --- |
| ① 作業元（SVG） | `godot/assets/appicon-src/icon_large.svg` / `icon_small.svg` | `build_logo.py` の出力 |
| ② 中間の PNG | `godot/assets/appicon-src/png/icon_<寸法>.png` | 16〜256 |
| ③ ゲーム用 | `godot/assets/appicon/icon.png` / `icon.ico` | 256px の PNG と、②を束ねた ICO |
| ④ 見え方の確認 | `godot/assets/appicon-src/preview.png` | ②を暗い地と明るい地に並べた1枚 |

作り直す手順は、`build_logo.py` を走らせて①を出し、`build_icon.ps1` で②〜④を焼く（[../tech/tools.md](../tech/tools.md)）。ICO は Godot が書けないので ImageMagick で束ねる。透過は残す（地の色は焼かない）。

## プロジェクトへの指定

| 設定 | 値 |
| --- | --- |
| `application/config/icon` | `res://assets/appicon/icon.png` |
| `application/config/windows_native_icon` | `res://assets/appicon/icon.ico` |
| エクスポートプリセットの `application/icon` | `res://assets/appicon/icon.ico` |

`config/icon` はエディタとプロジェクトマネージャが使い、ネイティブアイコンが無いときのウィンドウにも使われる。`windows_native_icon` を指すと、実行中のウィンドウとタスクバーが ICO の中から寸法に合う絵を選ぶ＝小さいときに1枚版が出る。ICO は Godot のリソースではないので、pck に入れるには非リソースのフィルタが要る（[../tech/build.md](../tech/build.md)）。実行ファイルに焼くアイコンはエクスポートプリセット側が持つ。

---

## 参考資料

- [logo.md](logo.md) — ロゴ（モチーフ・配色・寸法・作り方）
- [menu.md](menu.md) — 起動スプラッシュ（同じ紋章を使うもう一つのゲーム内画像）
- [../tech/tools.md](../tech/tools.md) — 生成スクリプトの起動方法
- [../tech/build.md](../tech/build.md) — 配布ビルド（同梱物の決め方）
