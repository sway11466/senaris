# BGM（音楽制作・組込）

BGM の制作方針と組込の運用。少ない曲を作り込み、ムード別に使い回す。翻訳アセットの運用（[../tech/i18n.md](../tech/i18n.md)）と同じく「素材パイプラインの取り決め」を扱う。

## 前提

- 制作者はゼロから作曲はしないが、楽譜が読め、ベースのメロディがあれば編曲・修整ができる。
- 配布はまず Steam（PC）＝商用。

## 方針

全曲を自作アレンジで賄う。AI 支援で出した旋律のたたき台を土台に、MuseScore で編曲・仕上げをする。たたき台はあくまでベースで、良し悪しの判断と仕上げは制作者が行う。

曲数はむやみに増やさず、ムード別のライブラリ（下記）を横断で使い回す。足りなくなったら新曲より先に、同じ `.mscz` からの編成違い（短調化・テンポ変更・楽器の増減）で派生させる＝統一感が出て元手も増えない。危機BGM（`crisis`）がこの派生の想定例。

## ツール

- Muse Hub — Muse 社の配布・更新ランチャー。下記を入れる入口。
- MuseScore Studio（旧 MuseScore 4）— 楽譜作成ソフト本体。制作・編集はこれ。
- Muse Sounds — 高品質音源（オーケストラ等）。Muse Hub 経由で入れる。
- ffmpeg — WAV から Ogg Vorbis への変換とループ加工（下記ワークフロー）。

## データ形式

| 形式 | 拡張子 | 役割 | 扱い |
|---|---|---|---|
| MuseScore 原本 | `.mscz` | 編曲の原本 | **必ず残す**（後で直せる） |
| MusicXML | `.musicxml` | 記譜の交換フォーマット | ベースメロディの受け渡し・編集の入口 |
| MIDI | `.mid` | 演奏データ | たたき台の取り込み・プレビュー |
| Ogg Vorbis | `.ogg` | 最終成果物 | Godot に載せる。原本から作り直せる生成物 |

- 原本 `.mscz` が正。`.ogg` は生成物だが、絵の PNG と同じくコミットする（置くだけで鳴る運用のため）。
- `.ogg` の中身は Ogg Vorbis 必須（罠）。`.ogg` はコンテナ名で、中身が Opus のファイルも同じ拡張子を名乗る。Godot 4 が読めるのは Vorbis だけ（Opus はインポートが `valid=false`）。MuseScore から直接 `.ogg` を出すと Opus になりうるので、必ず WAV 経由で ffmpeg 変換する。
- 書き出しの基準値: WAV（中間）＝44100 Hz／16bit PCM／ステレオ（Godot の既定ミックスレートに合わせる）。OGG Vorbis（最終）＝44100 Hz／`-q:a 6`。
- MIDI から取り込むとリズム解釈がずれることがあるので手直し前提。

## 制作ワークフロー

```
たたき台（AI支援で出した .musicxml / .mid）
  → 編曲（MuseScore、原本 .mscz で保存）
  → 書き出し（.wav＝可逆。44100Hz / 16bit / ステレオ）
  → ループ加工（ループ長で切り、切った残響を先頭に足す＝1コマンド）
  → 変換（ffmpeg で Ogg Vorbis → assets/bgm/{track_id}.ogg）
  → Godot 組込（.import に loop=true）
```

### ループ加工（ループ曲のみ）

MuseScore の書き出しは末尾に残響のしっぽが付く。これをループ長ちょうどに切り（切らないと1周ごとに無音の間が入る）、切り落とした残響を先頭に足し込む。理想的なループ音声は「1周ぶんの音」＋「前の周から染み出す残響」の重ね合わせなので、残響を捨てると継ぎ目で不連続が知覚される。

```
loop(t) = s(t) + s(t+L)   … t < 残響長（L＝ループ長）
        = s(t)            … それ以降
```

ループ長は楽譜から求める。

```
L = 小節数 × 1小節の拍数 × 60 ÷ テンポ(♩)
```

例: `menu` は 32小節・4/4・♩=100 → 32 × 4 × 60 ÷ 100 = **76.800秒**。

トリムと加算は1コマンドで済む（`atrim` が切り出しを兼ねる）。

```
ffmpeg -i in.wav -i in.wav -filter_complex \
  "[0]atrim=0:<L>,asetpts=PTS-STARTPTS[body];\
   [1]atrim=<L>,asetpts=PTS-STARTPTS,apad=whole_dur=<L>[tail];\
   [body][tail]amix=inputs=2:normalize=0:duration=first[out]" -map "[out]" looped.wav

ffmpeg -i looped.wav -c:a libvorbis -q:a 6 assets/bgm/{track_id}.ogg
```

`normalize=0` は必須（省略すると音量が半分になる）。`-q:a 6` は約 160kbps。

確認すること。

- 先頭に無音があれば加工前に落とす（`silencedetect` / `volumedetect`）
- 加工後のピークが元と変わっていない（＝加算でクリップしていない）
- 変換後に `ffprobe` で `codec_name=vorbis`（`OpusHead` なら失敗）

スティンガー（`victory` / `defeat`）はループしないので加工不要＝末尾の残響は残す。

適用条件は 残響長 < ループ長（既存曲は残響約3秒に対しループ 60〜77秒で余裕がある）。残響がループ長に近い曲は足し込む項が1つでは足りないので、MuseScore で64小節（2周）に複製して書き出し `[L, 2L]` を切り出す。

副作用として曲の頭に「前周の残響」が入る（初回再生時は本来存在しない音）。`menu` では -22dB 相当で、実聴で違和感なしを確認済み。

中間生成物（`.wav`）と MuseScore のバックアップ（`.mscbackup/`）はコミットしない。

### 音量を揃える — 統合ラウドネス -23 LUFS 目安

曲どうしの聞こえの大きさは統合ラウドネス（LUFS）で揃える。ピーク（dBFS）は歪みの有無を見る指標で、聞こえの大きさは測れない。目安は -23 LUFS。ゲーム業界の推奨（Sony ASWG-R001 の据置機向けは -23〜-24 LUFS）の範囲で、BGM は効果音が上に乗る前提なので放送より静かめのこの位置が理にかなう。

```
ffmpeg -i in.wav -af ebur128=peak=true -f null -     # I: が統合ラウドネス
```

MuseScore 側の作り方は、パート間のバランスを各トラックのフェーダーで作り、曲全体の音量を master で決める。ブーストは合算を押し上げて出口の余裕を食うので、バランスは下げる方向で組む。master フェーダーは書き出しに 1:1 で反映される（+2 dB 動かすと測定値も +2.0 動く）ので、全体音量の調整は master 1本で済む。

外れているとどうなるかの実例。`dungeon` は当初ティンパニのトラックだけ +12 dB で -17.4 LUFS ＝ 他より 5.6 LU 大きく、ピークも -2.9 dBFS まで来ていて効果音との合算で余裕が無かった。ミキサーを組み直して -23.4 LUFS / -9.8 dBFS に収めた。

組み込んだ曲の聴き比べ（曲どうしの音量差）とループの継ぎ目の確認は `sound_check` で行う（[../tech/tools.md](../tech/tools.md)）。本体と同じ再生経路を使うので、ここでの聴こえ方が実機と一致する。

## トラックライブラリ（ムード別・キャンペーン横断）

BGM は**ステージ単位**で流す。本作はマップそのものが戦場で1回の殴り合いが一瞬なので、**戦闘ごとに曲を切り替えず、攻撃・着弾は効果音（SFX）と戦闘演出で示す**（ネクタリス／Advance Wars 型。戦闘が別画面に遷移するファイアーエムブレム型とは分ける）。

**トラックはキャンペーンに属さない。** ムード（状況）別のライブラリとして育て、各ステージが JSON の `bgm` 欄で ID を選ぶ。同じようなマップ／状況なら別キャンペーンでも同じ曲を使い回す（FE 等の汎用マップ曲と同じ運用）。トラック ID もムード基準で命名し、キャンペーン接頭辞（`t1_` 等）は付けない。新しいムードが必要になったらライブラリに曲を足す。

| track_id | ムード（状況） | 雰囲気・主楽器 | ループ | 状態 |
|---|---|---|---|---|
| `title` | タイトル | 作品の顔。メインテーマ。オーケストラ | ○ | 未着手 |
| `menu` | メニュー／ステージセレクト | おおらか・爽やか・広がり。フルート＋弦アルペジオ。ニ長調 ♩=100 | ○ | 投入済み（76.800秒・ループ） |
| `journey` | 旅立ち・平原・冒険の始まり | 明るく前進。歩くベース＋速い和音変化。ニ長調 ♩=108 | ○ | 投入済み（71.111秒・ループ） |
| `raid` | 襲撃・防衛戦・切迫 | 弦オスティナート＋ティンパニ。ロ短調 ♩=126 | ○ | 投入済み（60.952秒・ループ） |
| `dungeon` | 洞窟・地下・探索 | ドローン＋まばらな旋律＋心拍。ホ短調 ♩=80 | ○ | 投入済み（96.000秒・ループ） |
| `forest` | 迷いの森・霧・エルフ領 | 6/8 の揺れる舞曲＋弔鐘。ト短調 ♩=150（旧 crisis v1 を転用） | ○ | たたき台あり（32小節） |
| `ruins` | 古代遺跡（ダンジョン系＋荘厳） | ドリア旋法のホルンコラール＋石柱の弦＋遠鐘。イ短調ドリア ♩=66 | ○ | たたき台あり（32小節） |
| `graveyard` | 墓地・アンデッド | 亡霊ワルツ（3拍子）。弔鐘＋弦の刻み。ニ短調 ♩=90 | ○ | たたき台あり（32小節） |
| `temple` | 邪神の神殿（静の畏怖） | パイプオルガン＋低い聖歌チェロ＋トライトーンの鐘。ホ・フリジア ♩=58 | ○ | たたき台あり（32小節） |
| `ritual` | 邪神の儀式（動の狂気） | 5拍子の呪術ドラム＋催眠リフ＋増2度の旋律。ニ・フリジアンドミナント ♩=112 | ○ | たたき台あり（32小節） |
| `boss` | 精鋭ボス戦（威圧・正面対決） | ホルンの雄々しいテーマ＋弦オスティナート＋重ティンパニ。ハ短調 ♩=140 | ○ | たたき台あり（32小節） |
| `boss2` | ボス戦（疾走・爽快の決闘） | フルート疾走リード＋駆動オスティナート。嬰ヘ短調 ♩=172 | ○ | たたき台あり（32小節） |
| `victory` | 勝利スティンガー | ファンファーレ（フルート＋ホルン他）。ニ長調 ♩=132・約10秒 | × | 投入済み（決着時に一発再生） |
| `defeat` | 敗北スティンガー | 嘆きの下降旋律（フルート＋弦）。ニ短調 ♩=84・約14秒 | × | 投入済み（決着時に一発再生） |
| `crisis` | 危機（状態切替用） | 警報型：裏拍の弦スタブ＋同音連打＋半音の軋み。イ短調 ♩=160 | ○ | たたき台あり（32小節・v2） |

割当（チュートリアル1）: st1–2 = `journey` ／ st3–5 = `raid` ／ st6–7 = `dungeon`（いずれもステージJSONに記載済み）。
割当例（チュートリアル2）: 序盤 = 既存ムード ／ 墓地ステージ = `graveyard` ／ 最終ステージ（ネクロマンサー）= `boss`。

> 「戦闘曲」を独立トラックとしては持たない。緊張感は**ステージ曲の出し分け＋下記の状態切り替え**で作る。個々の戦闘は SFX（[../tech/combat_scene.md](../tech/combat_scene.md) の演出）で示す。
> 使い回しで耳が飽きてきたら、キャンペーンの看板1曲だけ専用曲を足して対処する（派生で凌ぐ方針は §方針）。最初から作り分けない。

## 管理・運用（ディレクトリ・トラックID・ステージ指定）

絵の管理（[../art/overview.md](../art/overview.md)、`assets/units/` ＋ `assets/units-src/`、autowire）と同じ思想で揃える。効果音を作る時も `assets/sfx/`＋`assets/sfx-src/` を並べる。

### ディレクトリ — `xxx` / `xxx-src` の対

```
assets/
  bgm/                  ← ゲーム用 .ogg（Godot が読む）
    menu.ogg
    menu.ogg.import     ← ループ設定（loop=true）。生成物だがコミットする
  bgm-src/              ← 制作元（.gdignore を置いて Godot のスキャン対象外にする）
    .gdignore
    menu.mscz           ← 編曲の正（必須）。track_id 名でフラットに置く
    menu.musicxml       ← 受け渡し・たたき台（任意）
    menu.wav            ← 書き出し中間物（コミットしない＝.mscz から再生成できる）
    credits.md          ← 権利・ライセンス台帳（曲単位）
```

### トラックID — 規約 autowire

- 曲は**トラックID**（`journey` / `raid` / `title` / `victory` …＝上記ライブラリ表）で扱い、`assets/bgm/{track_id}.ogg` を規約で解決する（skin の画像 autowire と同型）。
- ファイルが**あれば**鳴る。**なければ無音＋開発ログ1行**（絵のプレースホルダの音版。ゲームは止めない）。曲が完成したら `.ogg` を置くだけで鳴り出す＝コード・データ変更なし。

### ステージ単位の指定 — ステージJSONの `bgm` 欄

```json
{ "name": "tutorial-st1",
  "bgm": { "main": "journey", "crisis": "crisis" } }
```

- スロット制：`main`（必須）＋ `crisis`（任意＝状態切替用。未指定なら切替イベントが来ても曲は変わらない）。将来 `intro` 等もスロット追加で対応。
- **フォールバック連鎖**：ステージの `bgm` → `campaign.json` の既定 → 全体既定。全ステージに書かなくても冒険譚単位の指定で済む。

### レイヤー配置

| レイヤー | 実体 | 責務 |
|---|---|---|
| `data` | `data/audio/bgm_catalog.gd` | トラックカタログ（ID→パス解決・autowire）と `bgm` 欄の読み取り。純ロジック |
| `application` | `application/bgm_director.gd` | ステージ開始時に `bgm` 欄から「鳴るべきトラックID」を決定（フォールバック連鎖）。`crisis` フラグ管理もここ |
| `presentation` | `presentation/ui/bgm_player.gd` | `AudioStreamPlayer`×2 の入れ替え。ID を受けて鳴らすだけ |

ステージJSON の `bgm` 欄は `StageLoader.parse_bgm` / `load_bgm`（会話・skin と同じく `BattleState` には入れない）、冒険譚既定は `CampaignCatalog` が `bgm` キーとして持つ。`main`（`presentation/main/main.gd`）が両者を `BgmDirector` に渡し、決まったIDを `BgmPlayer` に流す。

場面の切り替わりは2箇所だけ。ステージ開始（`_install_state`＝新規ロードと中断セーブ復元で共通）と、セレクトを開いたとき（`SelectScreen.opened` → メニュー曲）。同じトラックIDを指す遷移では鳴り続ける（頭出しに戻らない）。

### 曲の出し入れ — 旧曲はフェードアウト・新曲は頭出し

新曲はフェードインさせず素の音量で頭から鳴らす。旧曲だけ 1 秒でフェードアウトする。フェードインを掛けると曲の入りが聞こえないまま過ぎるため（-60dB から 1 秒で上げると 0.5 秒地点でまだ -30dB）。スティンガーの頭を殺さない扱いと同じ理由。

ステージ曲は盤が1枚描き切ってから始める（`main._start_stage_bgm_when_drawn`）。`_install_state` の中で鳴らすと、残りの盤生成と初回描画のぶん（実測 0.1〜0.3 秒）だけ曲が盤より先行する。

## Godot 組込

`AudioStreamOggVorbis` で読み、`AudioStreamPlayer` で再生する。ループはインポート設定の `loop` / `loop_offset` で持つ（`.import` に乗る＝絵と同じ管理感覚）。ステージ中ずっと流れる曲はループ必須。

`.import` の `loop` はインポート後に手で `true` へ直し、`.godot/imported/` の生成物を消して再インポートすると確実に反映される。`.import` 自体は生成物だがコミットする。

## 状態でBGMを切り替える（アダプティブ）

特定の状況で曲を切り替えられる。例: **敵が必殺技を使ったら危機BGMに変わり、以後クリア／敗北まで戻らない**。

- **方式**: まずは**旧曲のフェードアウト＋新曲の頭出し**（§曲の出し入れ）で十分。音楽的にきっちり繋ぎたければ Godot 4 の **`AudioStreamInteractive`**（クリップ＋遷移ルール。次の小節頭で切替）を使う。レイヤー加算（`AudioStreamSynchronized`）は素材コストが高いので後回し。
- **「永続」＝状態フラグ1個**: ステージ状態に `crisis: bool` を持ち、一度立てたら戻さない（曲がパタパタ切り替わる事故を防ぐ）。ステージ開始でリセット。
- **責務分担**は §レイヤー配置のとおり。`domain` は「敵が必殺技を使った」という事実をシグナルで出すだけ（音を知らない）。
- **危機BGMの用意**: ライブラリの **`crisis`**（警報型）を全ステージ共通で使う。ステージ固有の色が欲しくなったら、通常曲の短調化派生（同じ `.mscz` から）で亜種を足してもよい。ループ点整備は通常曲・危機曲の両方に必要。
- **セーブ**: 途中セーブがあるなら `crisis` フラグも保存する（ロード時に通常曲へ戻る事故を防ぐ。[../tech/gamesystem.md](../tech/gamesystem.md)）。
- **引き金の粒度**: 切替は“盤面が変わる級”のイベント（必殺技・ボス出現など）に絞る。弱いイベントで変えると唐突。

## 権利・ライセンス台帳

商用（Steam）前提。曲ごとの出自を台帳（`assets/bgm-src/credits.md`）に記録する。有料データ保護の方針（[../adr/ADR-0002-paid-data-protection.md](../adr/ADR-0002-paid-data-protection.md)）とは別で、こちらは他者の権利を侵さないための管理。

記録項目は 曲名（本作での用途）／出典／入手形式／ライセンス（商用可否・改変可否・クレジット表記の要否）／改変度。

現状はすべて自作アレンジなのでクレジット表記の義務は無い。第三者の素材を使うことになったら、表記が必要なものは指定文をそのまま Steam ページとゲーム内クレジットに載せる（自己流に変えない）。

## 現状 / TODO

- **たたき台あり（8曲・`.musicxml`／`.mid`）**: `forest`（旧 crisis v1 の転用）／ `ruins` ／ `graveyard` ／ `temple` ／ `ritual` ／ `boss` ／ `boss2` ／ `crisis`（v2・警報型）＝各32小節。いずれも MuseScore での仕上げ（強弱・味付け・ループ点整備）と `.ogg` 化は今後。ライブラリ表の未着手は `title` のみ（menu と共用するかは検討中）。
- 曲を追加したら本ドキュメントのライブラリ表と台帳（`assets/bgm-src/credits.md`）を更新する。
- **投入済み（6曲）**: `menu`（76.800秒）／ `journey`（71.111秒）／ `raid`（60.952秒）／ `dungeon`（96.000秒）＝いずれもループ長ちょうど・ラップアラウンド加算あり。`victory`（10.3秒）／ `defeat`（14.4秒）＝スティンガー（ループなし）。曲が未配置のステージは無音＋ログ1行で進む（autowire）。
- **スティンガーの鳴らし方**: `main._on_battle_finished` が決着時に `BgmPlayer.play_stinger` で一発再生（ステージ曲を素早く下げ、フェードインなしで頭から出す）。戦果票の印が落ちる瞬間に合わせる（[../gdd/uiux.md](../gdd/uiux.md) §決着の演出）。
- **全体既定は `map_calm`**（`BgmDirector.DEFAULT_STAGE_TRACK`）で、この ID の曲は無い＝ステージにも冒険譚にも `bgm` 指定が無いと無音になる。チュートリアル1は全ステージに `bgm` を書いたので現在は該当なし。既定を投入済みの曲に変えるか、`campaign.json` に既定を書くかは要判断。
