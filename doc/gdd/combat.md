# 戦闘システム

ターン制ヘックス戦術シミュレーション（ファンタジー）の戦闘解決の設計メモ。
ゲームシステムの核はネクタリス（Military Madness）を下敷きにしつつ、表現はオリジナルに置き換える方針。

---

## 設計の前提

- ジャンルはネクタリス系のヘックス制ターン戦術（ターン制・ヘックスマップ・戦術級）を下敷きにする。
- **生産なし**：ユニットの生産・補充は行わない。マップごとに与えられた戦力でやりくりする。
  - 結果として **1体1体が貴重** になり、損耗そのものが戦略的な重みを持つ。
  - 後述の「陣形スキル」でユニットを消費する判断も、この縛りがあることで意味を増す。
- **決定的（乱数なし）**：戦闘結果は乱数を含まず確定的に決まる。
  - 「盤面で噛み合わせて勝つ」コンセプトは詰将棋的な読みが核。乱数は読みを濁らせ、生産なしで貴重な1体が運で溶けるのはフラストレーション。
  - 決定的なら包囲・支援・地形・陣形スキルがそのまま実力になり、**中断セーブはシード不要・リプレイ完全一致・AI先読みも正確**（アーキの未決「戦闘の決定性」を決定的で閉じる）。
  - 代償として AI が弱いと作業ゲー化するため、**強敵ユニット・マップ構成・将来の高度AI**で担保する。

---

## 用語

- **ユニット** … 盤上の駒＝小隊1個。
- **兵数** … 1〜8。ユニットの残存兵数（8=満員、1=壊滅寸前、0=消滅）。
- **ユニット攻撃力 / ユニット防御力** … 兵1体あたりの基礎攻撃力・防御力。
- **対地 / 対空** … 攻撃側は **相手が飛行なら対空(`atk_air`)、地上なら対地(`atk_ground`)** を使う（防御は単一値で分けない）。実装済み。**`atk_air=0`＝対空不可**で、飛行を**攻撃対象に選べず**、飛行に殴られても**反撃しない（被反撃なし・経験+0）**。専用強ユニットのドラゴン等は飛行（→ [units.md](units.md)）。**対空の担い手は弓兵・飛行・魔法兵**（占領兵・歩兵・斥候の近接は `atk_air=0`）。
- **貫通率（`pierce`）** … 攻撃側が持つ「**相手の防御を無視する割合**」（0〜1）。**魔法兵＝0.5（防御半減）／物理＝0**。防御は単一値なので**対地・対空どちらの相手にも同じく効く**。効果は相手の防御が高いほど伸びる（＝対アーマー：弓兵＝対空・機動／魔法兵＝対アーマーで住み分け）。データ正本は `unit_type.csv` の `pierce` 列。**実装済み**（下記 [防御貫通](#防御貫通貫通率乗算攻撃側依存)）。
- **射程（下限〜上限）** … ユニットは `min_range`（最短）〜`attack_range`（最大）の距離を撃てる。`min_range=1`＝近接可、`min_range≥2`＝**懐に死角**（砲兵など＝隣接した敵を撃てない）。CSVは `range` 列に「`1`＝近接／`1-2`＝弓／`3-5`＝砲兵の死角」のレンジ表記で書く（`UnitType.parse_range`）。
- **近接攻撃 / 間接攻撃は「攻撃した距離」で決まる**（ユニット種別ではない）。**距離1＝近接**（防御側は反撃する・**支援**が乗る）／**距離≥2＝間接**（反撃なし・支援なし）。**包囲**・地形・経験は距離によらず乗る（＝囲まれた敵は間接でも脆い）。実装済み（`melee := 距離==1`）。
  - よって射程1-2の弓兵が隣接（距離1）で殴った一撃は**近接扱い＝反撃を受け、支援も乗る**。射程を保てば反撃なしで削れる、という位置取りの緊張が生まれる。
  - **反撃は「距離1」かつ「防御側がその距離1を狙える（`min_range≤1`）」ときだけ成立**。砲兵(`min_range≥2`)は懐に入られると反撃できない（被弾一方＝要護衛）。

---

## コアメカニクス

### 1. 戦闘補正（包囲・支援・地形）

基本ステータスに各補正を適用し、攻撃力と防御力の比から損害を算出する（**決定的＝乱数なし**。理由は[設計の前提](#設計の前提)）。

戦闘解決のルール:

- **先制なしの同時攻撃**。攻撃側・防御側が同時に攻撃し、防御側も反撃する。
- 損失は **戦闘前の兵数** を基準に双方へ同時に確定・適用する。→ **1回の戦闘で両者が全滅（相討ち）し得る**。
- **間接攻撃（距離≥2）には反撃が無い**（防御側は反撃で兵を失わない＝撃たれた側は経験+0）。近接/間接は攻撃した距離で決まる（[用語](#用語)）。実装済み。

#### 攻撃力・防御力の計算式

各補正を **乗算（経験・包囲・地形）＋ 加算（支援）** で適用する:

```
実効攻撃力 A ＝ 兵数 × ユニット攻撃力 × 経験 × 包囲 × 地形(攻) ＋ 支援(攻)
実効防御力 D ＝ 兵数 × ユニット防御力 × 経験 × 包囲 × 地形(防) ＋ 支援(防)
```

#### 失う兵数の計算式

同時攻撃。**戦闘前の兵数**で2方向を別々に計算し、両方を同時に適用する:

```
（X が Y を攻撃する場合）
Y失う兵数 ＝ clamp( round( k × Y兵数 × Aₓ^p / (Aₓ^p + D_y^p) ), 0, Y兵数 )
X失う兵数 ＝ clamp( round( k × X兵数 × A_y^p / (A_y^p + D_x^p) ), 0, X兵数 )   ← 反撃: Yの攻撃力 vs Xの防御力
```

- **端数は四捨五入**（`round`。0.5 は切り上げ、例: 6.5→7）。最後に `clamp` で 0〜相手の兵数に収める。
- `A^p/(A^p+D^p)` ＝ 相手の兵数のうち削る割合(0〜1)。**互角(A=D)は常に0.5**、戦力差がついた時だけ鋭くなる。防御0でも発散しない。
- **k（殺傷力）**: 全体の削り量。大きいと短期決戦、小さいと削り合い（位置取りの手数が増える）。
- **p（決定力）**: 戦力差の効き。**初期 p=2**（攻撃2倍→0.8 / 半分→0.2）。互角は p によらず常に0.5。
- 攻撃力と防御力は**独立**。攻撃を強化しても被反撃は変わらない（同じ防御力なら受ける損失兵数も同じ）。

#### 経験値補正（乗算）

**経験値＝レベル（1〜8）そのもの**。別の累積カウンタは持たず、レベルが直接たまる（原典ネクタリス同様）。**初期 Lv1 ＝ 補正なし（×1.0）**。攻撃・防御は共通係数。生き残るほど上がり、上昇幅も加速する（伸びしろが後半ほど大きい）。

`経験 ＝ 1 ＋ 下表の%`（例: Lv6 なら ×1.40）。

| Lv | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|----|---|---|---|---|---|---|---|---|
| 経験(+%) | +0% | +5% | +10% | +15% | +25% | +40% | +65% | +100% |

**入手ルール（原則: 戦ったら +1、倒したら さらに +1）**:

- **攻撃側**: 戦闘に参加で +1、相手を撃破で さらに +1（近接・間接とも）。
- **防御側**: **反撃が成立したら +1**、その反撃で相手を撃破で さらに +1。反撃できなければ **+0**。
  - 一般原則は「**反撃したユニットだけ経験を積む**」。間接攻撃を受けた側、対空能力なしで空ユニットに攻撃された側は反撃しない → **+0**。
  - 近接攻撃は必ず反撃が成立するため、近接の防御側は +1。
- 上限 **Lv8 で頭打ち**。**占領による加算は拠点占領の実装時に決める**【保留】。

| 状況 | 攻撃側 | 防御側 |
|---|---|---|
| 近接・両者生存 | +1 | +1 |
| 近接・攻撃側が撃破 | +2 | （消滅） |
| 近接・反撃で攻撃側が落ちた | （消滅） | +2 |
| 間接・防御側生存（反撃なし） | +1 | +0 |

実装は `domain/combat/combat.gd`（係数）／`domain/battle_state.gd`（入手）／`domain/unit/unit.gd`（保持）。

#### 包囲効果（乗算・被包囲ペナルティ）

- ファンタジー世界観に合わせ **「包囲」** と呼称（ZOCは一般的なウォーゲーム用語のため、リネームは法的な必要ではなくデザイン上の選択）。
- **囲まれた側の攻撃力・防御力が低下するデバフ**。式では「そのユニット自身」に掛かる自己係数 `包囲`。
  - ※「自分の攻撃が上がる」のではなく「**囲まれた敵が弱る**」点に注意。攻防どちらにも同じ係数が乗る。
- **段階式**にする（覆い具合に応じて連続的に効く。決定的ゲームなので、配置の工夫が連続的に報われる方が読み合いが深い）。

**判定（対象の周囲6ヘックスを分類）**:
- **占有** … 敵ユニットがそのヘックスにいる
- **ZOC下** … 空きだが敵に隣接している（敵の支配下）
- **盤外ヘックスは数えない**（盤端・隅は包囲されにくい＝防御的退避）

**包囲係数の式**（隣接する敵が2体以上で成立）:
```
隣接する敵ユニット < 2  → 包囲 ＝ 1.0（包囲不成立。1v1の通常攻撃では発動しない）
隣接する敵ユニット ≧ 2  → 包囲 ＝ clamp( 1 − (0.08 × 占有数 ＋ 0.04 × ZOC数), 0.10, 1.0 )
```

**パラメータ**: 占有 **0.08** ／ ZOC **0.04** ／ 成立は隣接 **2体**以上 ／ 係数の下限 **0.10**。実装は `domain/surround/surround.gd`。

- 攻撃能力のないユニットでも、隣接すれば包囲の頭数になる（→陣形スキルで行動完了したユニットも盤面に居る限り寄与）。
- **近接・間接の両方に効く**。包囲は「囲まれたユニット自身にかかる常時デバフ」で、攻撃種別に依存しない（囲んで止めた敵を間接で集中放火すると刺さる／囲まれた弓兵は射撃も弱る）。支援は近接のみ（後述）。

代表値（係数）:

| 状況 | 占有/ZOC | 包囲 | 同格2v1の損失：敵 / 自分※ |
|---|---|---|---|
| 敵1体だけ隣接 | 1 / – | ×1.00（不成立） | 4 / 4 |
| 隣り合う2体 | 2 / 2 | ×0.76 | 6 / 2 |
| 1つ飛び2体 | 2 / 3 | ×0.72 | 6 / 2 |
| 対角2体（最小包囲） | 2 / 4 | ×0.68 | 6 / 2 |
| 3体で囲む | 3 / 3 | ×0.64 | 7 / 1 |
| 6体で完全に塞ぐ | 6 / 0 | ×0.52 | 8 / 0 |

※ 攻撃側視点の「**敵の失う兵数 / 自分の失う兵数**」。例: `6 / 2` ＝ 敵を6減らし自分は2減る。全員 兵数8・攻防10/10、支援込み（囲んだ側面ユニットは攻撃支援も兼ねる）、k=1・p=2 のとき。
- **2体包囲＝6/2、体を足すほど階段状に殲滅へ**。`対角2体で6/2`は強いが、**満員2体を隣接させ続けるのは盤面コストが高い**（相手に的を絞らせ反撃も受ける）ので釣り合う。

#### 支援効果（加算）

- 隣接する味方による底上げ。**攻撃側・防御側の両方に効く**（攻撃側は攻撃力、防御側は防御力）。実効ステータスに**加算**される（乗算ではない＝味方が多いほど効く）。
  - **支援(攻)** ＝ Σ「**防御対象に隣接する自軍**」の `兵数 × ユニット攻撃力 × 0.25`（攻撃側を底上げ）
  - **支援(防)** ＝ Σ「**攻撃者に隣接する自軍**」の `兵数 × ユニット防御力 × 0.25`（防御側を底上げ）
  - 支援量は支援側の**兵数に比例**（弱った味方の支援は小さい）。経験値補正は含めない素の値。
  - **率は 0.25**（加算は隣接味方数ぶん積み上がり、放置すると乗算の包囲補正を食って全戦闘が一方的になるため、当初の0.5から下げた）。
- **防御側の上限**: 支援後の実効防御力は **支援前の実効防御力の2倍** まで。
- **近接攻撃のみ**。間接攻撃には乗らない（隣接していれば味方に支援を与える側にはなれる）。
- **包囲と表裏一体**: 防御対象に隣接させた側面ユニットは、敵を**包囲**しつつ自軍の攻撃を**支援**もする（同じ1体が両方に効く）。逆に攻撃者に隣接する敵の味方は、攻撃者を包囲しつつ守備側を支援する。配置1つで包囲・支援が同時に動く。実装は `domain/combat/combat.gd`。

#### 地形効果（乗算・攻防別）

- **攻撃・防御の両方に地形係数を持たせる**（攻防別の2係数）。罠地形＝攻撃強化、穴ぐら＝防御強化…のように地形の性格づけで戦略を広げる狙い。
- 各地形が `地形(攻)` と `地形(防)` を持つ。**ユニットが立っているヘックスの地形**が効く（攻撃側は自分の足元の地形(攻)、防御側は自分の足元の地形(防)）。

**地形はデータ駆動**。正本 `data/terrain/terrain.csv`（`id, char, atk, def, image`）→ `terrain.json`、`Terrain` カタログ（`data/terrain/terrain.gd`）が係数・ASCII文字・タイル画像ファイル名を提供。**地形を増やす＝terrain.csv に1行＋movement.csv に1列**（コード不変）。タイル画像は placeholder を `tools/gen_terrain_tiles.gd` で用意（色は生成時に1回決める＝データには持たない。アート確定後は同名PNGを差し替え）。地形idは文字列（`plain`/`plateau`/`forest`…）で、そのまま移動コスト表のキーになる。

**実装済みの地形（現在13種・真は `terrain.csv`）**。代表例:

| id | 地形 | char | 地形(攻) | 地形(防) | 性格 |
|---|---|---|---|---|---|
| plain | 平地 | `.` | ×1.0 | ×1.0 | 補正なし（基準） |
| plateau | 台地 | `P` | ×1.15 | ×1.15 | 高所（攻守有利） |
| forest | 森 | `F` | ×1.0 | ×1.2 | 守り寄り（地上は移動重い） |
| trap | 罠 | `T` | ×1.5 | ×1.0 | 攻め強化 |
| mountain | 山 | `M` | ×0.6 | ×0.6 | 戦いにくい障害（移動重い） |
| wall | 壁 | `#` | ×1.0 | ×1.0 | 全ユニット進入不可（ダンジョン） |
| fort | 拠点 | `O` | ×1.0 | ×1.2 | 拠点（守り強）。占領/出撃/回復は別レイヤー → [map.md](map.md) |

ほかに 道/荒地/茂み/柵/城壁/崖。係数・移動コストは terrain.csv / movement.csv 参照（**川・海・通路は廃止**、荒地を追加）。

- **拠点（fort/castle）は地形としては「防御の強いマス」**。占領・出撃（増援）・回復（休憩）の挙動は地形補正とは別レイヤーで、`domain/capture/`＋`BattleState` が担う（[map.md](map.md) 拠点・占領）。

- 実装: `Terrain`（係数・`attack_factor`/`defense_factor`）／`domain/battle_state.gd`（盤面の地形保持 `terrain_at`/`set_terrain`、idは文字列）／戦闘での参照は `domain/combat/combat.gd`。移動コストは [movement.md](movement.md)。

#### 防御貫通（貫通率・乗算・攻撃側依存）

- 攻撃側 X が **貫通率 `pierce_x`>0** を持つとき、その攻撃に対する防御側 Y の実効防御力に `×(1 − pierce_x)` を掛ける（相手の防御を貫く）。損害計算では `D_y` の代わりに下の `D'_y` を使う:

```
D'_y ＝ D_y × (1 − pierce_x)
```

- **魔法兵＝0.5＝防御半減**／物理（`pierce=0`）は据え置き。
- 防御は単一値なので、**対地・対空どちらの相手にも同じく効く**。
- 貫通は**攻撃した側の性質**＝反撃（Y→X）には **Y の貫通率**が同様に効く。
- **支援(防)加算・2倍上限を適用した後の実効防御力 D に掛ける**（＝支援も貫通の影響を受ける。実装でこの順に確定し、`tests/unit/test_pierce.gd` が上限→貫通の順を期待値で固定済み）。
- 効果は相手の防御が高いほど伸びる（対アーマー）。**弓兵＝対空・機動／魔法兵＝対アーマー**で住み分ける。
- **設計原則**：性能(type)は全スキンで共有されるため、貫通のような"術者らしさ"は**術者にしか化けない type（魔法兵）にのみ**載せる（→ [units.md](units.md) flavor-neutral 原則）。
- 実装: **配線済み**（`domain/combat/combat.gd` `defense_breakdown`＝内訳 dict の `pierce` に係数を保持／`unit_type.csv` の `pierce` 列＋JSON再生成／テスト `tests/unit/test_pierce.gd`）。

### 2. 陣形スキル

マップ上で特定のユニット配置が揃うと、スペシャルアクションが発動できる本作独自の概念。

#### 発動条件：編成レシピ

- ユニット種別ごとに定めた **所定の配置（編成レシピ）** が盤上で揃うと発動できる。レシピが増えるほど盤面構築と読み合いが豊かになる。
- **具体レシピの正本は [formations.md](formations.md)**（①三重詠唱＝ウィザード/ウィッチ3体・面殲滅／②ホーリーアリア＝占領兵5体・全体バフ／③神の裁き＝パラディン＋占領兵2・ボス狙撃…冒険譚ごとに追記）。

#### 発動ルール

- 発動は **プレイヤーの明示操作**（条件が揃っても自動では出ない）。
- 発動に参加したユニットは **行動完了** になる。
  - → 1体のユニットは **1ターンに1つの陣形スキルにしか参加できない**。
  - → 共有ユニット問題（複数の陣形スキル条件に同時該当する状況）が、追加ルールなしで自然に解決する。
  - → 「この3体を大魔法に回すか、魔法弓やほかの行動に残すか」という取捨選択が毎ターン発生する。

### 包囲と陣形スキルの関係

- **包囲＝下地（受動・連続）／陣形スキル＝決め技（能動・離散）** という役割分担で噛み合う。
- 両者は微妙に引っ張り合う：
  - 包囲は「敵の周囲に味方を固める」ほど強い。
  - 陣形スキルは「味方同士で特定の形を作る」必要がある。
  - 同時に満たしにくい局面が、そのまま戦術的な読み合いの深さになる。
- さらに、陣形スキルのために固めた味方は敵に包囲されやすくもなる（攻守の表裏）。

---

## 未決事項（このドキュメントの範囲）

- [x] 経験値テーブル・入手・保持（実装済み: Lv=経験値1〜8、反撃したら+1の原則。占領による加算は拠点占領実装時に確定）。
- [ ] 地形タイプの追加と具体係数（map.md と連携）。仕組みは実装済み（平地・台地のみ／`domain/terrain/terrain.gd`）。盤面への地形配置（ステージデータ化）と presentation での地形描画は未着手。
- [ ] 包囲効果の判定タイミング（行動完了ユニットの扱いを含む）。
- [ ] 陣形スキルレシピ表の規模と、プレイヤーへの提示方法／ユニット確定後の仮レシピ再設計。
- [x] 間接攻撃（レンジ・反撃なし）を持つユニット種別の導入（実装済み: `attack_range`、アーチャー追加。**支援なし・撃たれた側+0だが包囲は乗る**＝囲まれた敵への集中放火が刺さる）。
- [x] **最短射程（`min_range`）と距離ベースの近接/間接判定**（実装済み: CSV `range` を `1`/`1-2`/`3-5` 表記で下限〜上限に解く＝`UnitType.parse_range`。攻撃距離1＝近接（反撃・支援あり）／≥2＝間接。`min_range≥2` は懐に死角で反撃も不可。`battle_state.gd` の `can_reach`／`melee:=距離==1`。テスト `tests/unit/test_combat.gd`・`test_battle_state.gd`）。
- [x] **防御貫通（`pierce`）の戦闘適用**（実装済み: `D'=D×(1−攻撃側pierce)` を支援・2倍上限の後に適用。`combat.gd`／JSON再生成／`tests/unit/test_pierce.gd`）。

---

## 関連ドキュメント

- [concepts.md](../concepts.md) — コンセプト・IP方針・数値の扱い
- [units.md](units.md) — ユニット性能・対応表
- [map.md](map.md) — マップ・拠点・占領・勝敗条件・地形タイプ別係数
- [world.md](world.md) — 世界観
- [../tech/architecture.md](../tech/architecture.md) — レイヤー構成・戦闘の決定性とセーブの前提
- [../tech/gamesystem.md](../tech/gamesystem.md) — セーブ仕様
- [../../tools/combat_sim/combat_sim.gd](../../tools/combat_sim/combat_sim.gd) — 戦闘計算シミュレータ（開発ツール）。`combat_sim.tscn` をエディタで F6 実行。式は本体の Combat をそのまま呼ぶ＝画面の数字＝実戦の数字
