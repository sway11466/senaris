# マップ・拠点・ステージ

拠点／占領、エネミー＆ステージ構成といった「マップ側」の設計メモ。

> 関連: 戦闘解決は [combat.md](combat.md)／ユニット性能・対応表は [units.md](units.md)。

---

## 用語（冒険譚・ステージ・シナリオ）

物語まわりで混同しやすい3語を定義する。**冒険譚とステージは「入れ物」、シナリオはそこに乗る「中身（物語）」** という役割分担。【確定】

| 用語 | 意味 | 粒度 | データ上の実体 |
|---|---|---|---|
| **冒険譚**（campaign） | 物語で束ねた一連のステージ群。配布・選択の単位 | 大 | `data/stages/<冒険譚>/` ＋進行マニフェスト |
| **ステージ**（stage） | 1回の戦闘＝1マップ。読み込んで遊ぶ最小単位 | 中 | `<ステージ>.json`（盤面＋ユニット配置＋勝敗条件＋戦力供給モデル） |
| **シナリオ**（物語・演出） | 冒険譚やステージに乗る筋書き・ブリーフィング・会話。コンテナではなく**側面語**で、固有の粒度を持たない | — | 各粒度に付随（「この冒険譚のシナリオ」「ステージ3のシナリオ」のように使う） |

- 例: チュートリアルの「町がゴブリンに襲われる」物語＝**1冒険譚**、その各マップ＝**ステージ**、各マップの筋＝**シナリオ**。

---

## 勝敗条件

- 原典（ネクタリス）は **「敵全滅」または「敵拠点の占領」を50ターン以内に達成** で勝利。
- **本作の暫定実装【暫定】**:
  - **自軍全滅＝敗北**。相討ちで両軍が同時に全滅した場合も、自軍が消えていれば **敗北を優先**（原典は相討ち全滅の結末を明示しておらず、拠点占領＋ターン制限でこの事態を実質回避している。本作は未実装ぶんを素直に「負け」で塞ぐ）。
  - **敵全滅＝勝利**。
  - **拠点占領による勝利・ターン制限は未実装**（拠点占領を入れれば相討ち全滅はレアケース化する）。【未決】

---

## 拠点・占領

- **拠点＝城・砦（軍事拠点）** と定義する。
- 拠点は4つの機能を持つ: **①防御地形 ②占領（即時） ③出撃（ネクタリス方式） ④回復（休憩）**。

### 拠点＝地形（防御に強い）

- 拠点は **防御の強い地形タイプ**（`fort`＝砦・`castle`＝城）として `terrain.csv` に持つ。攻×1.0／**防×1.5**（守り特化・要チューニング）。移動は地上コスト1（普通に入れて占領できる）。
- 見た目・地形補正・移動コストは既存の地形システムに乗る（[combat.md](combat.md) 地形効果／[movement.md](movement.md)）。占領・出撃・回復のロジックは地形とは別レイヤー（後述）で持つ。

### 占領（即時）

- **占領できるのは兵種「占領兵」**（cleric / priest / monk / bishop / paladin ＝ `can_capture` true）。兵種が占領兵なら占領可、という対応で揃える（unit_type.csv の兵種列と `can_capture` を一致させる）。
- そのうち **ステージ設計の軸になる3アーキタイプ**を据える（残りの priest＝高火力・monk＝俊足 も占領兵として併用できる）:
  - **クレリック**：安価で脆い（数で押す／使い捨て寄り）。
  - **ビショップ**：高耐久で保持向き（占領した拠点を守る）。
  - **パラディン**：戦えるエリート（占領しつつ前線でも機能）。
- **占領＝即時**: `can_capture` ユニットが敵・中立の拠点hexに**進入した瞬間**に、その拠点の所属が自軍へ移る。別コマンドは作らない（入る＝占領）。駐留敵がそのhexに居る間は進入できない（倒してから入る）。

### 出撃（ネクタリス方式・寝返り／解放）

- 拠点は中に **控えユニット（garrison）** を抱える。これが世界観上の「拠点に囚われていた**捕虜**」で、**占領すると garrison がそのまま自軍の出撃プールになる**（＝寝返り／解放）。
- **出撃はプレイヤー操作**: 自軍拠点の garrison から1体を選び、**拠点の隣接する空きhexへ出す**。出撃の移動は **1歩のみ**＝出した駒はそのターンは行動完了。次ターンから通常行動。
- **解放される駒はステージ側で指定できる**（シナリオ要求）。既定は「駐留ユニットのミラー」だが、ステージで別途指定可（例: ゴブリンに囚われた町人を **ノービス×2** として解放）。garrison の内容＝種別・数をステージデータで持つ。
  - これが **ドラゴンを占領で奪えない理由**にもなる：ドラゴンは敵専用で **対応する味方ユニットが存在しない** ため、garrison／解放対象にできない。
- **空の拠点**（garrison が空）を占領しても出てくる駒は無い＝所属が変わるだけ（無害に処理）。現スコープでは占領勝利・敵AI出撃が無いため、空拠点の占領は実質ノーオペ。

### 回復（休憩）

- **自軍所属の拠点hexに自軍ユニットが乗っていると、次の自軍ターン開始時に兵数が max まで全回復**（ネクタリスの拠点修復）。
- 中立・敵拠点では回復しない（自軍所属が条件）。位置ベース＝拠点に居てターンをまたげば回復（行動の有無は問わない）。**兵数のみ回復、経験Lvはそのまま**。
- 出撃の garrison（拠点の“中”）とは別軸。拠点hexの“上”に乗った1体が回復対象。

### 残る論点【未決】

- garrison から出撃する駒の兵数＝**満員(8)・Lv1で確定**（解放捕虜は満員で出る）。
- 寝返り数の上限・敵AIの拠点出撃ロジックは後回し（tutorialでは敵拠点出撃を使わない）。

---

## ステージデータの構成

- ステージは JSON データで記述する（ASCII地形グリッド＋ユニット配置）。データは `data/`、それを盤面(BattleState)に組み立てるローダー `StageLoader` は `application/`（data＋domain の両方に依存するため）。詳細 → [../tech/architecture.md](../tech/architecture.md)。
- **冒険譚（キャンペーン）ごとにフォルダ**で束ねる: `data/stages/<冒険譚>/<ステージ>.json`。
  - 1つの冒険譚＝数ステージ構成を想定し、その単位でフォルダに放り込む。
  - 例: 動作確認用のデモは `data/stages/demo/demo.json`。
- 【未決】冒険譚内のステージ順序・進行管理（マニフェスト等）、ステージ選択。当面は読込先を決め打ちで1枚。

### 戦力供給モデル（独立／継承）

ステージがプレイヤー戦力をどう用意するかを、**ステージ（冒険譚）ごとに切り替えられる**ようにする。【方針確定】「与えられた戦力でやりくりする」（[combat.md](combat.md) 生産なし）の"与えられ方"を、シナリオに応じて変える仕組み。

- **独立（fresh）＝既定**: そのステージが戦力を丸ごと配給する。前ステージの結果を引き継がない。チュートリアル各話のように1話完結のステージはこれ。
- **継承（carryover）**: 前ステージの**生存ユニットを持ち越す**（経験Lv・残兵数つき）。ダンジョン攻略・連戦シナリオ向け。ステージ側で増援を足してもよい。
- データ上はステージのフィールドで宣言する想定（例 `roster: "fresh" | "carryover"`）。冒険譚マニフェストと合わせて進行管理する。

**継承時に決める細目【未決】**:
- 何を継ぐか＝**生存ユニットのみ**／経験Lvは保持／**兵数は残ったまま（回復なし）**か、ステージ間で一部回復させるか。
- キーユニットを失って詰む（ソフトロック）対策＝継承ステージに増援を置くか、敗北救済をどう設計するか。
- セーブ（[../tech/gamesystem.md](../tech/gamesystem.md)）との接続（どのステージから再開し、戦力スナップショットをどう持つか）。

## エネミー／ステージ設計

- **テーマ別リスキン方式**：フィールドのテーマ（古代遺跡・死霊の洞窟…）ごとに、**同ステータス・別名のエネミーセット**を用意する。【方針確定】
  - 標準ロスター ＝ **テーマ0** と位置づける（→ [units.md](units.md) の対応表）。
  - マップに実際に出る分だけ命名すればよい（全テーマ×全種を先に作り込む必要はない）。

---

## 未決事項（このドキュメントの範囲）

- [ ] 生産なし × 種別依存レシピゆえの、マップ側のユニット配給設計（必要種別が供給されない問題への対処）
- [x] 拠点占領の中核設計（占領＝即時／出撃＝ネクタリス方式・1歩／回復＝休憩／garrison満員8／空拠点ノーオペ）。詳細は上記「拠点・占領」。残: 寝返り数の上限・敵AIの拠点出撃は後回し。
- [ ] 専用強ユニット（ドラゴン）を占領で奪えないルール（「対応する味方がいないため garrison にできない」で裏付く）の実装確認
- [ ] 戦力供給モデルの継承時ルール詳細（持ち越す対象・兵数回復・ソフトロック対策・セーブ連携）
- [ ] マップの「設計上の特徴」をどこまで引き継ぐか（具体配置はオリジナル）
