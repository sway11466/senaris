# 戦闘演出シーン

2ユニットの戦闘を見せる演出画面の設計。戦闘の解決（損害計算）は [../gdd/combat.md](../gdd/combat.md)／domain 側で確定済みで、本シーンはその結果を表示するだけ（数値を再計算しない）。使う画像スロットは [../art/overview.md](../art/overview.md)、立ち絵の作画方針は [../art/units.md](../art/units.md) §3.3。

盤の見た目は3Dハイブリッド（[../adr/ADR-0003-board-3d-hybrid.md](../adr/ADR-0003-board-3d-hybrid.md)）。演出も同じ3/4俯瞰に寄せ、地形の上に隊列が並ぶ地続き感を出す。

## 配置

- 左右は陣営で固定：プレイヤー左・敵右（攻撃側/防御側で入れ替えない）。立ち絵は向きを陣営に焼き込む（プレイヤー＝右向き／敵＝左向き）ので反転はしない。
- 背景は守り手（防御側）のタイル地形で1枚選ぶ。地形ID→`combat_bg` を autowire（全ユニット共通・ユニットスロットとは別系統）。攻撃側の地形は背景に使わない。
- 整備優先：野戦主力 plain→forest→mountain→plateau/wasteland。拠点/防壁系（fort/wall/rampart/fence）は「拠点戦」共通背景1枚でまとめる。road/bush/trap/cliff は後回し。
- 未用意地形のフォールバックは無地背景（地形テクスチャで代用しない）＝どの地形が未準備か一目で分かるようにする（気付ける穴あき運用）。

## 兵数の表示（隊列スロット）

- 各陣営の立ち絵（`combat`）を、その時点の兵数（1〜8・[../gdd/combat.md](../gdd/combat.md)）ぶん複製して並べる。同じ1枚を使い回し、スロットの表示ON/OFFで数を合わせる。兵数は戦闘ルールで上限8＝オーバーフロー処理は不要。
- 隊列テンプレ：後列3／中列2／前列3（計8）の楔形で中心（激突点）に正対。隊長は後列中央。
- スロット番号＝生存優先度。表示は slot1..兵数。損害は前列から消える（前が最初に落ち、隊長は最後まで残る）＝落ちる順 前列[8→7→6]→中列[5→4]→後列翼[3→2]→隊長[1]。
- 特別ユニット：slot1（後列中央）を `combat_hero` にし、残りを base の `combat`（従者）で埋める。英雄は最後まで残る。
- 3/4俯瞰の奥行き：前（中心寄り）ほど下・大きく、奥ほど上・小さい。隊長は後列だが小さくなりすぎないよう scale は個別調整可。
- 座標は陣営ボックス内の正規化値（0〜1・x は中心へ向かうほど1／敵側は 1−x でミラー）。実px はシーン解像度で決める＝下表は相対値の叩き台（微調整は実装時）。

| slot | x(奥0→前1) | y(上0→下1) | 列 |
|---|---|---|---|
| 1（隊長） | 0.18 | 0.55 | 後 |
| 2 | 0.15 | 0.30 | 後 |
| 3 | 0.15 | 0.80 | 後 |
| 4 | 0.50 | 0.40 | 中 |
| 5 | 0.50 | 0.66 | 中 |
| 6 | 0.85 | 0.30 | 前 |
| 7 | 0.85 | 0.55 | 前 |
| 8 | 0.85 | 0.80 | 前 |

- 立ち絵が未用意のスキンはプレースホルダ（ベタ塗り＋フルネーム）。主要ユニットから順次差し込む（autowire＝置くだけ切替）。

## 動き・エフェクト

- キャラのフレームアニメはしない（立ち絵は静止）。動きはシーン側で付ける：
  - 命中の瞬間に画面シェイク。
  - 被弾側の隊列にフラッシュ。
  - 攻撃エフェクト：損害は攻守同時なので、双方が自分の `combat_effect`（ユニットごと1枚）を相手の隊列上に重ねる（攻撃側→防御側／防御側の反撃→攻撃側）。攻撃主体を分かりやすくするため攻撃側を先・反撃を少し遅らせて出す。片方向になるのは間接攻撃（反撃なし）だけ＝下記 特殊ケース。
- 損害は攻守同時（相討ちあり・[../gdd/combat.md](../gdd/combat.md)）。数値は「敵の失う兵数／自分の失う兵数」を表示。
- 補正（経験・包囲・地形・支援・状態）は、効いたものをアイコン/バッジの有無で軽く示すだけ。数式の内訳は演出では出さず、攻撃前プレビュー（現行と同じ数式表示・[../gdd/combat.md](../gdd/combat.md)）に委ねる＝演出=結果、プレビュー=根拠。

## 特殊ケース

- 間接攻撃（`min_range≥2`・反撃なし・[../gdd/combat.md](../gdd/combat.md)）：攻撃側エフェクト→防御側被弾のみの片方向（攻撃側は無傷・被弾演出なし）。
- 全滅（片側0）：消滅側フェードアウト。相討ち（両0）も両方フェードで、専用の決着演出は設けない。
- 支援ユニット（隣接自軍）は演出に出さない（対峙は2ユニットのみ）＝上の補正アイコンで示すに留める。

## フロー

1. 戦闘確定（domain が損害を算出）→ 本シーンへ結果を渡す。
2. 開幕：背景＋両隊列（戦闘前の兵数）を表示。
3. 演出：シェイク＋フラッシュ＋エフェクト、損害数の提示。
4. 兵数を戦闘後の値へ更新（消えた分の隊列スロットを畳む）。
5. 閉じる（退場アニメは無し）。盤へ戻る。

## テンポ・スキップ

- プレイヤーの攻撃はフル演出。クリック/キーでいつでも結果へスキップできる。
- AI手番の戦闘は既定で短縮（結果ダイジェスト・シェイク最小）。連戦（1手番に複数戦闘）はキューで詰めて流す。
- グローバル設定「演出：フル／短縮／オフ」の3段。オフでも結果（損害数）は必ず出す。

## 未決事項

- [ ] 隊列スロットの実px（シーン解像度・立ち絵サイズ確定後）とアニメのタイミング値。
- [ ] `combat_bg` の作画スペック（画角・解像度）と「拠点戦」共通背景の内容。
- [ ] 演出速度設定のUI配置（システムメニュー／オプション）。
